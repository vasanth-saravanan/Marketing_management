import React, { useEffect} from 'react';
import { View} from 'react-native';

export function Logout({navigation} : any) {
    useEffect(() => {
        //navigation.navigate("StackOne" , { screen : "Login"})
      });
    return (
        <View></View>
    );
}
